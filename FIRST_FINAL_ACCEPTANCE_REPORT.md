# DISE 1차 최종 인수 테스트 보고서

- 실행일: 2026-09-26 (Asia/Seoul)
- 대상: 본부 GUI와 실제 통합 분석 경로
- 독립 시험 메일: `mail_body/body_data/email1.eml` ~ `email8.eml`
- 정답: 정상 1, 2, 4, 5, 7 / 피싱 3, 6, 8
- 원칙: 위 8건은 10,000건 문맥 모델의 학습 자료로 사용하지 않고 독립 시험에만 사용함

## 1. 최종 결론

핵심 실행 요구사항은 1차 인수 시험을 통과했다. 8개 독립 메일의 **통합 판정은 8/8 정답과 일치**했고, GUI가 사용하는 실제 분석 경로에서 문맥 ML, URL·HTML, 공식 사이트 탐색, 헤더·DNS, 첨부파일 검사가 함께 실행됐다.

다만 다음 세 항목은 완전 충족으로 표현할 수 없다.

1. 10,000건이 합성 데이터이므로 실제 환경 일반화 성능은 아직 입증되지 않았다.
2. 공식 홈페이지 HTML과 목적지 HTML을 짝지어 평가하는 ML은 양쪽 페이지를 모두 수집한 경우에만 실행된다. 공식 페이지를 확보하지 못한 메일은 기본 HTML 구조와 다른 객관적 증거로 판정한다.
3. 현재 Windows에서는 AhnLab V3가 주 백신이라 Defender 사용자 지정 검사가 비활성화된다. 이 경우 ClamAV와 자체 정적 검사가 실행되며, Defender 비활성 상태를 악성 탐지로 표시하지 않는다.

## 2. 8개 독립 메일 판정 결과

| 메일 | 정답 | 통합 판정 | 규칙 점수 | 문맥 ML | 검토 필요 | 결과 |
|---|---|---|---:|---:|---|---|
| email1 | 정상 | `no_signal` | 0 | 0.4023 | 아니오 | 통과 |
| email2 | 정상 | `no_signal` | 0 | 0.6928 | 아니오 | 통과 |
| email3 | 피싱 | `suspicious` | 20 | 0.6463 | 예 | 통과 |
| email4 | 정상 | `no_signal` | 0 | 0.2553 | 아니오 | 통과 |
| email5 | 정상 | `no_signal` | 0 | 0.0528 | 아니오 | 통과 |
| email6 | 피싱 | `suspicious` | 25 | 0.7853 | 예 | 통과 |
| email7 | 정상 | `no_signal` | 0 | 0.3051 | 아니오 | 통과 |
| email8 | 피싱 | `suspicious` | 0 | 0.3514 | 예 | 통과 |

이 결과는 ML 점수만 임계값으로 자른 결과가 아니다. 다음 두 사례가 결합 방식이 의도대로 작동했음을 보여준다.

- email2는 문맥 ML 점수가 0.6928로 비교적 높지만 객관적 위험 증거가 없어서 정상으로 유지됐다. ML의 단독 오탐을 막았다.
- email8은 문맥 ML 점수가 0.3514로 낮고 기존 규칙 점수도 0이지만, `p0lice.kr`과 실제 경찰 공식 도메인의 혼동 가능성을 공식 사이트 탐색·도메인 증거가 잡아 피싱으로 분류했다. ML의 단독 미탐을 다른 엔진이 보완했다.

원본 결과는 `analysis_result/final-acceptance-20260926T131431/acceptance-summary.json`에 있다.

## 3. 요구사항별 검증

### 3.1 헤더·본문·구조 분석과 문맥 ML 결합 — 충족

- 모델 `dise-semantic-synthetic-v2-groupcv`가 정상 로드돼 8건 모두 `semantic_status=ok`를 반환했다.
- 모델은 10,000건의 제목·본문 문맥을 학습했고, 헤더·URL·HTML·도메인·첨부파일 증거와 통합 판정에서 결합된다.
- 문맥 점수는 보조 증거로 실제 판정에 반영되지만 단독으로 정상 또는 피싱을 확정하지 않는다.
- email3과 email6에서는 문맥과 객관적 위험 신호가 함께 작동했다.
- email2와 email8은 각각 ML 오탐과 ML 미탐을 통합 증거가 교정했다.

### 3.2 새로운 메일에 대한 일반화 — 기능 충족, 성능 입증은 부분 충족

- 파일명이나 email1~8의 번호를 외우는 규칙 없이 분석 내용과 구조를 입력으로 사용한다.
- 1~8번을 학습에서 제외한 채 최종 통합 판정 8/8을 얻었다.
- 그러나 8건은 표본이 작고 10,000건은 합성 데이터다. 따라서 이 결과를 실제 환경 정확도 100%로 해석할 수 없다. 이후 실제 라벨 메일을 독립 시험군으로 누적해야 한다.

### 3.3 URL 추출과 중복 정리 — 충족

- 본문 표시 문자열과 HTML `href`에서 URL을 추출한다.
- 같은 호스트·출처의 반복 링크를 그룹으로 묶어 GUI의 동일 URL 반복 표시를 줄였다.
- 리디렉션과 최종 목적지를 별도 증거로 보존한다.

### 3.4 목적지 페이지 기본 HTML 분석 — 충족

- 허용된 HTTP(S) 목적지에 직접 접속해 응답 HTML을 수집한다.
- JavaScript 실행, 폼 제출, 하위 리소스 실행 없이 정적 구조만 분석한다.
- 폼, 비밀번호 입력, 스크립트, iframe, 외부 호스트, 전송 목적지 등 구조 특징을 계산한다.
- 시험 중 Notion, Google Forms, korea.kr, hacktheon.org, h4ckt5st.com 등의 페이지 수집·구조 분석 경로가 실행됐다.
- 접속 실패는 악성으로 단정하지 않고 실패 원인을 증거에 남긴다.

### 3.5 공식 홈페이지 탐색과 구조 비교 — 부분 충족

- 고정 JSON 기관 목록에만 의존하지 않고, 메일에서 관측한 기관·도메인 단서를 이용해 공식 후보를 탐색한다.
- 목적지와 공식 후보 양쪽 HTML을 수집할 수 있으면 태그 구성과 폼·입력 구조를 비교하고 HTML pair ML을 실행한다.
- email4에서는 Notion 목적지와 공식 구조를 확보해 HTML pair ML 점수 0.02084를 계산했다. 기준 0.7828 미만으로 정상 구조로 처리됐다.
- email8에서는 목적지 페이지 DNS가 실패했지만 공식 경찰 사이트와 혼동 도메인 관계를 객관적 증거로 잡았다.
- 공식 후보 또는 HTML을 확보하지 못하면 pair ML은 `skipped`가 된다. 이때 기본 HTML 분석은 계속되지만 공식 구조 ML 비교까지 완료됐다고 표시하지 않는다.

### 3.6 SPF·DKIM·DMARC와 DNS — 충족

- DNS 조회 성공과 발신자 인증 성공을 구분한다.
- 원문 `Authentication-Results`에 `pass`가 있어도 신뢰할 수 있는 발행 경계를 확인하지 못하면 안전 근거로 사용하지 않는다.
- 상세 내부 상태 `unverified_pass`는 위조 가능 원문 pass를 구별하기 위한 값이며, GUI에는 `판정 제외 · 원문 pass의 발행 출처 확인 자료 없음`으로 설명한다.
- 인증 자료가 없다는 이유만으로 최종 판정을 `판정 유보`로 만들지 않는다. 다른 위험 신호가 없으면 `탐지된 위험 신호 없음`, 위험 신호가 있으면 `주의`로 통합 표시한다.

### 3.7 첨부파일 악성코드 검사 — 충족, Defender 계층은 환경 제한

- 자체 정적 검사, ClamAV, Defender의 결과를 각각 구분한다.
- ClamAV 1.5.4 portable 런타임과 서명 DB를 첫 실행 때 내려받아 준비하는 경로를 구현했다.
- email1의 JPG 2개는 각각 자체 검사와 ClamAV에서 `clean`으로 판정됐다.
- Defender 종료 코드 2를 더 이상 악성 탐지로 오해하지 않는다.
- 현재 Defender는 V3 때문에 `disabled`로 보고되며, 이 상태는 경고나 악성 판정으로 바뀌지 않는다. V3는 운영체제 실시간 보호 계층으로 유지된다.
- 첨부파일에 정답 라벨이 없어도 서명 검사와 정적 구조 분석은 수행된다. 알려지지 않은 악성코드를 완벽히 보장하는 기능은 아니다.

### 3.8 GUI와 사용자 표현 — 충족

- 실제 GUI 버튼, 진행 상태, 결과 탭을 사용하는 통합 시험을 수행했다.
- 최종 결과는 `통합 판정`으로 표시되고, 과거의 포괄적 `판정 유보` 문구는 제거됐다.
- ML, 인증, HTML 수집 실패를 숨기지 않되 실패 자체를 정상 또는 악성으로 오판하지 않는다.
- 엔진별 근거에는 실제 판정에 사용하거나 사용 경계를 설명하는 항목만 남겼다.

### 3.9 인컨텍스트 API·키워드·PKL 잔재 정리 — 충족

- 메인 판정 경로에서 과거 키워드 JSON과 `phishing_knowledge_base.pkl` 의존을 제거했다.
- spaCy·KoNLPy를 메인 런타임에서 제거했다.
- GUI 통합 시험은 지식 베이스 파일이 새로 생성되지 않는 것도 확인한다.

### 3.10 재학습 가능성 — 충족

- 10,000건 데이터의 train/validation/test 분리를 유지한다.
- 모델 재학습기와 후보 평가 경로를 제공한다.
- 추가 데이터는 라벨, 원문 또는 파싱 가능한 본문, 분할 정보가 필요하다.
- 재학습 모델은 검증을 거친 뒤 메인 모델로 교체한다. 합성 데이터 내부 점수 1.0만으로 실제 성능을 확정하지 않는다.

## 4. 자동 시험 결과

### 핵심 런타임 시험

- 12개 핵심 모듈, 총 88개 단위 테스트: 전부 통과
- 실제 GUI 통합 시험: 전부 통과
- GUI 통합 사례: 인증 자료 없음, 위조 pass, 첨부 URL, 연속 두 번 실행, Razor 일치, Razor 오류, Razor 시간 초과
- 연속 실행 결과 디렉터리 분리, GUI 버튼·상태·진행률 복구, 지식 베이스 미생성도 확인
- 최종 출력: `ALL PASSED`

### 전체 저장소 테스트의 환경 제한

전체 발견 시험은 262개이며 결과는 다음과 같다.

- 통과: 223
- 오류: 37 — 현재 Windows 실행 토큰에 심볼릭 링크 생성 권한이 없어 `WinError 1314` 발생
- 실패: 2 — 한국어 임시 파일명의 현재 셸/환경 인코딩 차이로 manifest 이식성 비교가 불일치

37개 오류와 2개 실패를 기능 성공으로 숨기지 않는다. 이들은 메일 판정 실행 경로의 실패가 아니라 학습 manifest의 경로·이식성 방어 테스트 환경 문제다. 관리자 권한 또는 Windows 개발자 모드와 UTF-8 테스트 환경에서 별도로 재검증해야 한다.

## 5. 남은 작업 우선순위

1. 실제 수집 메일을 개인 정보 제거 후 독립 시험 세트로 늘려 합성 데이터 편향을 측정한다.
2. 공식 홈페이지 후보 탐색 성공률과 목적지·공식 HTML pair 생성률을 기록해 HTML ML 적용 범위를 넓힌다.
3. HTML pair 모델용 독립 라벨 데이터로 threshold와 오탐률을 재검증한다.
4. Windows 개발자 모드 또는 심볼릭 링크 권한을 확보해 전체 262개 테스트를 다시 실행한다.
5. UTF-8 파일명 환경에서 manifest 이식성 테스트 2건을 재검증한다.

## 6. 인수 판단

**1차 본부 GUI 통합 버전은 시연과 다음 개발 단계로 진행할 수 있다.** 핵심 분석 경로와 사용자 제공 8개 메일은 통과했다. 논문에서 실제 환경 성능을 주장하려면 합성 10,000건 및 8개 메일 결과와 별도로, 더 큰 실제 독립 시험 세트의 성능을 추가해야 한다.
